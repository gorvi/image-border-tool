# 项目开发规范 (RULES)

## 1. 自动功能规范
### 1.1 自动高亮 (Auto Highlight)
- **默认启用**：`highlight_enabled_var` 必须默认为 `True`。
- **实时生效**：当功能启用时，任何文本输入或变更都必须**立即触发**关键词检测与高亮，严禁要求用户重新勾选才能生效。
- **触发时机**：
  - 文本内容变更时 (Debounced)
  - 样式参数变更时
  - 功能开关启用时

## 2. 交互规范
### 2.1 危险操作确认
- **清除文字**：必须包含 **二次确认** (Two-step confirmation) 流程，防止误操作。

## 3. 开发流程规范
### 3.1 代码自检 (Self-Check)
- **提交前检查**：每次修改代码后，必须检查修改涉及的变量是否已正确初始化。
- **引用完整性**：新增UI控件或逻辑时，确保对应的成员变量已在 `__init__` 中定义。

### 3.2 代码重构与模块化 (Refactoring & Modularity)
- **文件长度限制**：单个代码文件严禁超过 **1000行**。
- **功能修改原则**：在修改、调试或新增功能时，如果涉及的方法所在文件已接近或超过限制，**必须**将该功能或相关方法提取到新的独立文件（如 `modules/` 目录下）中。
- **禁止误删**：重构时务必小心，确保不误删现有逻辑，尤其是看起来冗余但实际用于不同场景的代码。

### 3.3 视觉一致性 (Visual Consistency)
- **虚线/边框统一**：预览（Canvas）、单图导出（Export）和批量导出（Batch Export）必须使用 **统一的绘制算法或参数**，确保视觉效果（如虚线间距、点大小、图案覆盖层级）在所有场景下完全一致。
